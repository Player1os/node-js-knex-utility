// Load app modules.
import { Model } from '.../src/model'

/**
 * A class version of the Model class with the generic parameters set to the most general type.
 */
export class GeneralModel extends Model<object, object, object> {}
